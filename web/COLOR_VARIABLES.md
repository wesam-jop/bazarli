# 🎨 متغيرات الألوان - Color Variables

## 📋 نظرة عامة

تم إنشاء نظام متغيرات ألوان موحد على مستوى المشروع بالكامل. يمكنك تغيير جميع ألوان الموقع من مكان واحد فقط!

## 🎯 المتغيرات الأساسية (8 متغيرات)

### 1. **PRIMARY** - اللون الأساسي
- **الاستخدام**: الأزرار الرئيسية، الروابط، التأكيدات
- **الموقع**: `web/resources/css/app.css` - السطر 27-30
- **القيم الحالية**:
  ```css
  --color-primary: #FF7A32;        /* برتقالي */
  --color-primary-light: #FF9D66;
  --color-primary-dark: #E65A1F;
  --color-primary-hover: #FF8F4D;
  ```

### 2. **SECONDARY** - اللون الثانوي
- **الاستخدام**: الخلفيات، النصوص الثانوية، الحدود
- **الموقع**: `web/resources/css/app.css` - السطر 38-41
- **القيم الحالية**:
  ```css
  --color-secondary: #F4F4F4;       /* رمادي فاتح */
  --color-secondary-light: #FFFFFF;
  --color-secondary-dark: #E0E0E0;
  --color-secondary-hover: #E8E8E8;
  ```

### 3. **ACCENT** - اللون المميز
- **الاستخدام**: العناصر المميزة، الإشعارات، التأكيدات الإضافية
- **الموقع**: `web/resources/css/app.css` - السطر 49-52
- **القيم الحالية**:
  ```css
  --color-accent: #3b82f6;         /* أزرق */
  --color-accent-light: #60a5fa;
  --color-accent-dark: #2563eb;
  --color-accent-hover: #60a5fa;
  ```

### 4. **SUCCESS** - لون النجاح
- **الاستخدام**: الرسائل الناجحة، التأكيدات الإيجابية، الحالات الناجحة
- **الموقع**: `web/resources/css/app.css` - السطر 60-63
- **القيم الحالية**:
  ```css
  --color-success: #10b981;        /* أخضر */
  --color-success-light: #34d399;
  --color-success-dark: #059669;
  --color-success-hover: #34d399;
  ```

### 5. **WARNING** - لون التحذير
- **الاستخدام**: التحذيرات، التنبيهات، والحالات المعلقة
- **الموقع**: `web/resources/css/app.css` - بعد SUCCESS
- **القيم الحالية**:
  ```css
  --color-warning: #f59e0b;        /* أصفر/برتقالي */
  --color-warning-light: #fbbf24;
  --color-warning-dark: #d97706;
  --color-warning-hover: #fbbf24;
  ```

### 6. **ERROR** - لون الخطأ
- **الاستخدام**: الأخطاء، الرسائل السلبية، والحالات الملغاة
- **الموقع**: `web/resources/css/app.css` - بعد WARNING
- **القيم الحالية**:
  ```css
  --color-error: #ef4444;         /* أحمر */
  --color-error-light: #f87171;
  --color-error-dark: #dc2626;
  --color-error-hover: #f87171;
  ```

### 7. **INFO** - لون المعلومات
- **الاستخدام**: المعلومات، الإشعارات، والتأكيدات
- **الموقع**: `web/resources/css/app.css` - بعد ERROR
- **القيم الحالية**:
  ```css
  --color-info: #3b82f6;          /* أزرق */
  --color-info-light: #60a5fa;
  --color-info-dark: #2563eb;
  --color-info-hover: #60a5fa;
  ```

---

## 🔧 كيفية تغيير الألوان

### خطوات التغيير:

1. **افتح الملف**: `web/resources/css/app.css`
2. **ابحث عن**: `@theme {` (السطر 9)
3. **غيّر القيم** في المتغيرات الثمانية:
   - `--color-primary`
   - `--color-secondary`
   - `--color-accent`
   - `--color-success`
   - `--color-warning`
   - `--color-error`
   - `--color-info`
4. **احفظ الملف**
5. **أعد بناء المشروع**: `npm run build` (أو `npm run dev` للتطوير)

### مثال على التغيير:

```css
/* قبل التغيير */
--color-primary: #9333ea;  /* بنفسجي */

/* بعد التغيير */
--color-primary: #ef4444;  /* أحمر */
```

**النتيجة**: جميع الأزرار والروابط الرئيسية ستتحول للأحمر تلقائياً!

---

## 💻 كيفية الاستخدام في الكود

### في Tailwind CSS Classes:

```jsx
// استخدام Primary
<button className="bg-primary-500 text-white hover:bg-primary-600">
  زر رئيسي
</button>

// استخدام Secondary
<div className="bg-secondary-800 text-white">
  خلفية ثانوية
</div>

// استخدام Accent
<span className="text-accent-500">
  نص مميز
</span>

// استخدام Success
<div className="bg-success-500 text-white">
  نجاح!
</div>
```

### في CSS المخصص:

```css
.my-custom-button {
  background-color: var(--color-primary);
  color: white;
}

.my-custom-button:hover {
  background-color: var(--color-primary-hover);
}
```

### في JavaScript/React (Inline Styles):

```jsx
const buttonStyle = {
  backgroundColor: 'var(--color-primary)',
  color: 'white'
};

<button style={buttonStyle}>زر</button>
```

---

## 📊 الألوان المتاحة لكل متغير

كل متغير يحتوي على درجات مختلفة (50-950):

- `primary-50` إلى `primary-950`
- `secondary-50` إلى `secondary-950`
- `accent-50` إلى `accent-950`
- `success-50` إلى `success-950`

### الاستخدام:

```jsx
// فاتح جداً
<div className="bg-primary-50">...</div>

// فاتح
<div className="bg-primary-200">...</div>

// متوسط (الأساسي)
<div className="bg-primary-500">...</div>

// داكن
<div className="bg-primary-700">...</div>

// داكن جداً
<div className="bg-primary-900">...</div>
```

---

## 🎨 أمثلة على التغيير

### مثال 1: تغيير إلى ألوان زرقاء

```css
--color-primary: #3b82f6;        /* أزرق */
--color-primary-light: #60a5fa;
--color-primary-dark: #2563eb;
--color-primary-hover: #60a5fa;
```

### مثال 2: تغيير إلى ألوان خضراء

```css
--color-primary: #10b981;        /* أخضر */
--color-primary-light: #34d399;
--color-primary-dark: #059669;
--color-primary-hover: #34d399;
```

### مثال 3: تغيير إلى ألوان حمراء

```css
--color-primary: #ef4444;        /* أحمر */
--color-primary-light: #f87171;
--color-primary-dark: #dc2626;
--color-primary-hover: #f87171;
```

---

## 🔄 التوافق مع الكود القديم

النظام متوافق مع الكود القديم:
- `purple-*` → يستخدم `primary-*`
- `slate-*` → يستخدم `secondary-*`
- `blue-*` → يستخدم `accent-*`

**لا حاجة لتغيير الكود القديم!** ✅

---

## 📝 ملاحظات مهمة

1. **تأكد من الحفظ**: بعد تغيير الألوان، احفظ الملف
2. **أعد البناء**: في الإنتاج، شغّل `npm run build`
3. **اختبر التغييرات**: تأكد أن جميع الصفحات تعمل بشكل صحيح
4. **الألوان المتناسقة**: اختر ألوان متناسقة مع بعضها
5. **التباين**: تأكد من وجود تباين كافٍ للنصوص

---

## 🎯 الخلاصة

✅ **8 متغيرات رئيسية** يمكن تغييرها من مكان واحد  
✅ **سهولة الاستخدام** في Tailwind CSS  
✅ **متوافق مع الكود القديم** (purple, blue, green, yellow, red, orange, slate)  
✅ **تغيير فوري** لجميع ألوان الموقع  
✅ **ألوان الحالة** موحدة في جميع الصفحات (pending, confirmed, preparing, delivered, cancelled)  

**الملف**: `web/resources/css/app.css`  
**الموقع**: داخل `@theme { ... }`

---

## 🚀 ابدأ الآن!

افتح `web/resources/css/app.css` وغيّر الألوان كما تريد! 🎨
